server {
    listen 80;
    server_name localhost;
    keepalive_timeout   0;
    allow       127.0.0.1;
    allow       ::1;
    deny        all;
    access_log  off;

location /nginx-status {
        stub_status     on;
        }
}
